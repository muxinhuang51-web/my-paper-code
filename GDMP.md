2603.20582
Generative Diffusion Model for Risk-Neutral Derivative Pricing
基于扩散模型的衍生品定价套利

---
能刻画复杂分布
传统模型常假设收益近似正态，但真实市场有偏度、厚尾、波动聚集。扩散模型对复杂分布拟合能力更强，不容易被单一分布假设限制。

路径级生成能力强
它不仅能给终点分布，还能生成整条价格路径，所以对路径依赖产品（如亚式、障碍期权）更自然。

与随机微分方程框架兼容
扩散模型本身就有 SDE 视角，和金融里常用的连续时间建模语言一致，便于把无套利、风险中性等约束嵌进去。

比很多生成方法更稳定
相对 GAN，扩散模型训练通常更稳定，模式崩溃风险小，在金融这种对尾部和罕见情形敏感的任务里更重要。

可用于“数据驱动 + 理论约束”结合
可以先从历史数据学到高阶结构，再通过风险中性修正去做定价，这比纯参数模型更灵活，也比纯黑盒更可解释一些。

适合高维和多因子场景
在多资产联合分布、相关结构、条件生成（按市场状态生成）方面有潜力，适合复杂组合风险分析。



### 核心问题
金融市场有两套"测度"：
- **真实世界 (ℙ)**：我们看历史数据学到的分布，包含真实的收益率 μ
- **风险中性世界 (ℚ)**：定价用的分布，收益率变成无风险利率 r

两个世界的方差一样（都是 σ²Δt），**但均值不同**：
- 真实世界一步收益：$\mu \Delta t$
- 风险中性世界一步收益：$r \Delta t$

### 扩散模型的两步
1. **正向加噪**：原始收益 → 加N步噪声 → 纯噪声
2. **反向去噪**：纯噪声 → 学会去N步噪声 → 生成收益

反向去噪靠的是 **score function**，即：$s_t(y) = \nabla_y \log p_t(y)$  
意思就是"在这个时刻，嘈杂分布的'山峰'朝哪个方向"。

### 关键转换：从 ℙ 到 ℚ
设在第 t 个扩散时刻，嘈杂的收益分布是高斯分布 $N(\mu_t, \sigma_t^2)$：
- 在 ℙ 下：均值是 $\mu_t^ℙ = \bar{\alpha}_t \cdot m^ℙ$（"嘈杂后"的真实均值）
- 在 ℚ 下：均值是 $\mu_t^ℚ = \bar{\alpha}_t \cdot m^ℚ$（"嘈杂后"的风险中性均值）
- **方差一样**：$\sigma_t^2 = \bar{\alpha}_t v_0 + (1 - \bar{\alpha}_t)$

**这两个高斯分布的 score 差异就是一个常数！**

$$s_t^{ℚ}(y) - s_t^{ℙ}(y) = \frac{\bar{\alpha}_t}{\sigma_t^2}(m^ℚ - m^ℙ)$$

为什么？因为对高斯分布 $N(\mu, \sigma^2)$，score 就是 $\frac{\mu - y}{\sigma^2}$，两个 score 相减时 y 项抵消，只留下均值差。

### 魔法修正：epsilon shift
在 DDPM 反向去噪时，每一步都用神经网络预测"这里有多少噪声需要去掉"，记为 $\epsilon_\theta$。

**修正方案很简单**：
$$\epsilon_\theta^{ℚ}(y,t) = \epsilon_\theta^{ℙ}(y,t) - \delta_t$$

其中修正项是：
$$\delta_t = \frac{\bar{\alpha}_t(1-\bar{\alpha}_t)}{\sigma_t^2}(m^ℚ - m^ℙ)$$

**通俗说**：
- 训练时用真实数据学 $\epsilon_\theta^{ℙ}$（学会从"真实世界的嘈杂"恢复）
- 推理时减去一个固定的 $\delta_t$（告诉模型"假装收益率是 r 不是 μ"）
- 这样生成的收益路径就**自动满足风险中性**

### 为什么有效：鞅性质
如果生成的一步收益 $Y_h$ 满足 ℚ 分布，即按 risk-neutral drift $r$ 生成，那么：
$$E^ℚ[e^{-rt_h} S_{t_h}] = S_0$$

这就是"贴现价格是鞅"，定价的前提。DDPM 的 epsilon shift 正好确保这一点。

### 保留了什么
- **方差**：原封不动 ✓
- **高阶矩**（偏度、峰度）：也保留了 ✓
- **路径依赖性**：整条路径都遵循 ℚ，不只是终点 ✓

### 简化总结
| 步骤 | 说明 |
|------|------|
| 1. 用历史数据训练 DDPM | 学会"从真实世界的噪声恢复收益" |
| 2. 计算 $\delta_t = \bar{\alpha}_t(1-\bar{\alpha}_t) \frac{\mu - r}{\sigma_t^2}$ | "真实-风险中性"的漂移差 |
| 3. 每个反向去噪步都减去 $\delta_t$ | 改变"去噪方向"，转向风险中性 |
| 4. 推理时生成完整路径 | 得到满足无套利的合成价格轨迹 |
| 5. 蒙特卡洛求期权价值 | 对路径取平均，贴现得到期权价格 |

## 多头周线的执行方式

用风险控制代替频繁交易

### 1. 标的选择 
- 只选流动性好的股票，比如大市值、低换手成本、公告冲击相对可控的标的
- 先避开 ST、停牌频繁、涨跌停约束强、基本面变化特别剧烈的股票

### 2. 观察频率
- 用周线作为主信号，减少日内噪声
- 每周固定一天做决策，比如周五收盘后计算，周一开盘执行
- 不追求每天换仓，核心是让信号有时间走出来

### 3. 进场条件
- 周线趋势向上
- 量能没有明显衰竭
- 回撤没有破坏中期结构
- 如果再叠加 DDPM，就用它生成的上行概率、尾部风险和未来几周路径分布来筛选

### 4. 持有与退出
- 只要周线结构没坏，就持有
- 退出条件优先看：趋势跌破、波动突然放大、组合层面风险超限，或者模型给出下行尾部概率上升
- 不因为一天的波动就换仓，避免被噪声洗掉

### 5. 仓位控制
- 单票仓位不要过高，周线策略更适合分散加中等仓位
- 可以按信号强弱分层建仓，比如强信号全仓位，弱信号半仓位
- 组合层面设置最大回撤和行业暴露上限

### 6. 和 DDPM 怎么结合
- DDPM 不直接替代选股逻辑，而是做路径和尾部风险评估
- 例如：模型判断未来 4 周上涨路径占优，但尾部风险也高，那就降低仓位
- 如果周线趋势向上且 DDPM 生成的下行分布较窄，可以提高持有置信度

### 7. 最简单的执行框架
| 步骤 | 做法 |
|------|------|
| 选股 | 先筛流动性好的股票 |
| 看周线 | 趋势、量能、回撤三项一起看 |
| 用模型 | DDPM 估计未来几周的路径分布 |
| 定仓位 | 按信号强弱分层建仓 |
| 周度复盘 | 每周只做一次正式调整 |

### 一句话版本
多头周线的执行，本质上就是：用周线判断大方向，用模型判断路径风险，用仓位控制来处理不确定性。
