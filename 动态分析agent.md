2604.24212

## 核心思路

### 迭代调试循环

1. 获取程序反馈
2. 根据反馈修复
3. 重新运行并迭代

传统调试器主要进行低层次的逐行交互。

基于 LLM 的调试器则进行细粒度动态分析：记录函数调用及其内部状态，帮助定位问题并生成修复方案。
* 这个调试器面向细致的理解和推理
收集执行轨迹 → 分析错误原因 → 修改代码 → 重新运行验证。
传统直接使用pdb接口低效的，不使用则是失败的

## FLT 数据结构

捕捉单个函数的参数、返回值和状态变化。

```text
FLT {
    fid         # 当前函数
    fid_caller  # 调用者
    args        # 输入参数
    ret         # 返回值
    tau         # 时间或执行轨迹
}
```
