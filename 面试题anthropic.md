[text](https://github.com/anthropics/original_performance_takehome)
面向某处理器，某算法的编译器级别优化：
* 占满计算资源与减少计算
接下来分三个层次讲清楚：
# 问题分析：
## 处理器
目标机器是一台单核的VLIW+SIMD的处理器, 大概你也可以理解为一个类似于TPU这样的简单的推理芯片ASIC的抽象
VLIM：超长指令字
SIMD：向量化，一条指令对多种数据做同一种操作
engine:alu,valu,load等功能单元
Core(单核):核心有独立的sractch(1536个32位字)，trace,pc
scratch(暂存)：通用计算器堆，常量池，手动管理的缓存


                                程序 (Program)
              [指令0][指令1][指令2]…[指令N]   每条=一个 bundle
                            │
                    每周期取一条 (pc++)
                            ▼
        ┌─────────────────── Core ───────────────────┐
        │  PC ──► 取指/译码/分发                        │
        │                                             │
        │  ┌────────┬────────┬────────┬────────┬────┐ │
        │  │  ALU   │  VALU  │  LOAD  │ STORE  │FLOW│ │   ← 5 个引擎
        │  │  ×12   │  ×6    │  ×2    │  ×2    │×1  │ │     同周期并行
        │  └───┬────┴───┬────┴───┬────┴───┬────┴─┬──┘ │
        │      │        │        │        │      │    │
        │      └────────▼────────▼────────▼──────┘    │
        │             Scratch 寄存器堆                 │
        │          (1536 × 32bit 词)                  │
        └───────────────┬─────────────────────────────┘
                        │ load / store 指令（mem↔scratch）
                        ▼
        ┌──────────────────────────────────────────────┐
        │    Mem 内存（32bit 词数组）                     │
        │  头7词 | 树节点 | 输入索引 | 输入值 | 额外空间    │
        └──────────────────────────────────────────────┘

## 算法
并行二叉树遍历

```python
for h in range(inp.rounds):
        
```
┌─────────────────────────────────────────────────────┐
│                开始一轮（共 rounds 轮）               │
└───────────────────────┬─────────────────────────────┘
                        │
        ┌───────────────┴───────────────┐
        ▼                               ▼
  游走者 i=0                      游走者 i=batch-1
  ┌───────────────────┐           ┌───────────────────┐
  │ 读 idx, val       │           │ 读 idx, val       │
  │ val ^= t.values[idx]│    ...   │ val ^= t.values[idx]│
  │ val = myhash(val) │           │ val = myhash(val) │
  │ 判奇偶选左右孩子   │           │ 判奇偶选左右孩子   │
  │ 越界则回绕到根     │           │ 越界则回绕到根     │
  │ 写回 idx, val     │           │ 写回 idx, val     │
  └───────────────────┘           └───────────────────┘
                        │
                        ▼
            所有游走者完成，进入下一轮